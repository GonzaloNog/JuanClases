/*
 * Solución Tarea 2.1.2
 * Tema 2.1 Construcción de software modular
 * 2º Ingeniería Informática
 * CUNEF Universidad
 * Prof. Dr. Jose L. Salmeron
 */

#include <iostream>
#include <string>
using namespace std;

/*
class Travel {
private:
    string destination;  // private attributes for the destination of the travel
    int duration;        // private attributes for the duration of the travel in days
    float cost;          // private attributes for the total cost of the travel in dollars

public:
    // Constructor that takes the values of the destination, duration, 
    // and cost of the travel and initializes them in the corresponding attributes
    Travel(string d, int dur, float c) {
        destination = d;
        duration = dur;
        cost = c;
    }

    // Public methods for accessing the attributes
    string getDestination() {
        return destination;
    }

    int getDuration() {
        return duration;
    }

    float getCost() {
        return cost;
    }

    // Public methods for modifying the attributes
    void setDestination(string d) {
        destination = d;
    }

    void setDuration(int dur) {
        duration = dur;
    }

    void setCost(float c) {
        cost = c;
    }

    // Public method for displaying the travel information to the console
    void displayTravel() {
        cout << "| " << destination << "\t | " << duration << " days\t| $" << cost << "\t|" << endl;
    }
};

int main() {
    // Create two objects of the "Travel" class with the initial values 
    // of the destination, duration, and cost of the travel
    Travel travel1("London", 7, 1000.50);
    Travel travel2("Huelva", 5, 800.25);

    // Modify some of the member variables using the "set" methods
    travel1.setCost(1200.75);
    travel2.setDuration(7);

    // Print the travel information to the console using the "displayTravel()" method
    cout << "-------------------------------------------------" << endl;
    cout << "| " << "Destination\t | Duration\t| Cost\t\t|" << endl;
    cout << "-------------------------------------------------" << endl;
    travel1.displayTravel();
    travel2.displayTravel();
    cout << "-------------------------------------------------" << endl;

    return 0;
}
*/
int main() {
    int a = 0;

    cout << a;

    return 0;
}
